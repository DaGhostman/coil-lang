[package]
name = "machine"
version = "0.1.0"
edition = "2024"

[features]
trace = []
stress = []

[dependencies]
common = { path = "../common" }
libffi = "4.0.0"
libloading = "0.8.6"
parser = { path = "../parser" }
rustc-hash = { version = "2.1.1" }
