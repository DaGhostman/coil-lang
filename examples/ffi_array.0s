// Sum a zero-script int array via C `sum_array`. Expected output: `15`.

use ffi::*;
use ffi::types::*;

fn main() {
    let lib = dload("libsum.so");
    let id = declare(lib, "sum_array", (Ptr, Int), Int);
    let arr = [1, 2, 3, 4, 5];
    print "%i", invoke(lib, id, (arr, 5));
}
