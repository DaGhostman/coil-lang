// Expected output: 42hi1.5true(3,4)99
//
// `%v` displays values through the `Show` typeclass. Builtin instances
// cover int/float/string/bool/unit; user types can `impl Show<T>`.

enum Point {
    Point { x: int, y: int },
}

impl Show<Point> {
    fn show(Point p) -> string {
        return format "(%i,%i)", p.x, p.y;
    }
}

fn show_it<T: Show>(T x) {
    print "%v", x;
}

fn main() {
    show_it(42);
    show_it("hi");
    show_it(1.5);
    show_it(true);
    print "%v", Point::Point { x: 3, y: 4 };
    let s = format "%v", 99;
    print "%s", s;
}
