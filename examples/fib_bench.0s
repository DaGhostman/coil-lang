// CPU / dispatch regression bench (release `poop` / perf_metrics).
// Correctness smoke uses examples/fib.0s with fib(10).
fn fib(int n) -> int {
    if n <= 2 {
        return 1;
    }

    return fib(n - 1) + fib(n - 2);
}

fn main() {
    print "%i", fib(32);
}
