[package]
name = "regalloc_pressure"
version = "0.1.0"
edition = "2021"
description = "Experiment C: measure register pressure on existing examples via linear-scan simulation"

[workspace]  # empty [workspace] table — opt OUT of any parent workspace

[[bin]]
name = "regalloc_pressure"
path = "src/main.rs"
