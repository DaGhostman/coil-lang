[package]
name = "gc_root_set"
version = "0.1.0"
edition = "2021"
description = "Experiment B: Validate register-VM GC root-set walk under callee-saves"

[workspace]  # empty [workspace] table — opt OUT of any parent workspace

[[bin]]
name = "gc_root_set"
path = "src/main.rs"
