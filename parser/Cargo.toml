[package]
name = "parser"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
reporting = { path = "../reporting" }
chumsky = { version = "0.12", features = ["lexical-numbers", "memoization", "pratt"] }

[dev-dependencies]
proptest = "1"
