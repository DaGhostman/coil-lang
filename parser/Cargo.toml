[package]
name = "parser"
version = "0.1.0"
edition = "2024"

[dependencies]

common = { path = "../common" }
rand = "0.9.0"
rustc-hash = { version = "2.1.1", features = ["rand"] }
scanner = { path = "../scanner" }
