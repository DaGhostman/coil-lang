/target
tmp/
flamegraph.svg
perf.*
callgrind.out*
out.hyc
*.so
*.o
THREADS_PLAN.md
# Per-machine hook state (session ids / mtimes) — regenerated, never source.
.cursor/hooks/state/
# Local coil-stdlib checkout (CI clones here; sibling `../coil-stdlib` also works).
.deps/
# Local coil-regex checkout (CI clones here; sibling `../coil-regex` also works).
/coil-regex/
