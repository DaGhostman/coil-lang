/target
tmp/
flamegraph.svg
perf.*
callgrind.out*
out.hyc
*.so
*.o
# Cwd-relative IO fixtures (Windows has no /tmp).
coil_*.bin
coil_adventure_save.dat
THREADS_PLAN.md
# Per-machine hook state (session ids / mtimes) — regenerated, never source.
.cursor/hooks/state/
# Local coil-stdlib checkout (CI clones here; sibling `../coil-stdlib` also works).
.deps/
