// Exercised by `zero-script test` — success-path assert / Result edges.
fn main() {
    assert(true)?;
    assert(1 + 1 == 2, "arithmetic")?;
    print "%s", match assert(true) {
        Result::Ok(_) => "ok",
        Result::Err(_) => "bad",
    };
}
