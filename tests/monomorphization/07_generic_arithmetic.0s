// Test: Generic with arithmetic operations
// Expected: Should emit correct instructions for int/float

fn add<T>(T a, T b) -> T {
    return a + b;
}

fn main() {
    let int_sum: int = add<int>(10, 20);
    print "%i", int_sum;
}
