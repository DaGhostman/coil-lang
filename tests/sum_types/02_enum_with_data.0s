// Test: Enum with data fields
// Expected: Should compile variants with fields

enum Colors {
    RGB(int, int, int),
    CMYK(float, float, float, float)
}

fn main() {
    let rgb = Colors::RGB(255, 128, 0);
    let cmyk = Colors::CMYK(1.0, 0.5, 0.0, 0.0);
    print "Created colors";
}
