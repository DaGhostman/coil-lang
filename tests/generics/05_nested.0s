// Test: Nested generic calls
// Expected: Should handle generics calling other generics

fn inner<T>(T x) -> T {
    return x;
}

fn outer<T>(T x) -> T {
    return inner<T>(x);
}

fn main() {
    let a: int = outer<int>(42);
    let b: float = outer<float>(3.14);
    print "%i %f", a, b;
}
