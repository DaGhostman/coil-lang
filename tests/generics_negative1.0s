// Test file for generics implementation
// Negative tests - these should produce type errors

fn identity<T>(T x) -> T {
    return x;
}

fn main() {
    // Test 1: Type mismatch - assigning float to int
    let x1: int = identity<float>(42);
    print "%i", x1;
}
