[package]
name = "compiler"
version = "0.1.0"
edition = "2024"

[dependencies]
ariadne = { version = "0.6.0", features = ["auto-color"] }
common = { path = "../common" }
parser = { path = "../parser" }
regex = "1.11.2"
