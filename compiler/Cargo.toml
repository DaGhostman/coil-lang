[package]
name = "compiler"
version = "0.1.0"
edition = "2024"

[features]
dump = []

[dependencies]
common = { path = "../common" }
parser = { path = "../parser" }
tinyrand = "^0.5"
rustc-hash = "^2.1"

